export const MAP_ANIMATION_DURATION = 1000;
export const MAP_DELAY_BEFORE_ANIMATION_DURATION = 500;

export const DELTA_FOR_SNGLE_CAR = {
  latitudeDelta: 0.04,
  longitudeDelta: 0.04,
};

// TODO: create function for calculating map's delta for bunch of cars
export const DELTA_FOR_BUNCH_OF_CARS = {
  latitudeDelta: 6,
  longitudeDelta: 6,
};

export const MAP_INITIAL_REGION = {
  latitude: 45.5,
  longitude: 39,
  longitudeDelta: 10,
  latitudeDelta: 10,
};

// Use my own number for whats up, in other case I can't open a chat with mock number
export const DEFAULT_NUMBER_FOR_MESSAGE = "+380663312127";
